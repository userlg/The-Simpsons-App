version: '3.8'

services:
  simpsons-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: simpsons-vue-app
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
